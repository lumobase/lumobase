{
  "name": "snake",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf dist && rm -f snake.lumo",
    "dist": "npm run clean && mkdir dist && cp -r lumobase.jsonc icon.png public dist/",
    "build:lumo": "npm run dist && cd dist && zip -r ../snake.lumo .",
    "dev": "vite dev"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
